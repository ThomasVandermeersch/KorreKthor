include ../header.pug

div(class="container page create")
    div(class="progress mt-3 mb-2")
        <div class="progress-bar" role="progressbar" style="width: 20%;" aria-valuenow="20" aria-valuemin="0" aria-valuemax="100">20%</div>
    
    h2 Etape 1 : Importer les élèves

    form(action="/create/sendList" method="POST" enctype="multipart/form-data")
        div(class="row mt-3")
            div(class="col-12")
                label(for="fileForm" class="form-label") Veuillez entrer le fichier Excel avec la liste d'élèves.
            div(class="col-6 col-md-9")
                input(class="form-control" id="fileForm" type="file" name="studentList" required)
                
                if messages.errormsg
                    div(class="alert alert-danger mt-2" role="alert")= messages.errormsg
            
            div(class="col-5 col-md-3")
                input(class="btn btn-warning float-end" type="submit", value="Submit file")

include ../footer.pug
