include header.pug

div(class="container page create")

    h1 Envoyez votre email

    form(action="/sendComplainEmail" method="POST")
        div(class="form-group row")
            label(class="col-sm-2 col-form-label") Destinataire :
            div(class="col-sm-10")
                input(type='email' value=email name='email')
        
        div(class="form-group row")
            label(class="col-sm-2 col-form-label") Objet :
            div(class="col-sm-10")
                input(type=text value=object size="20" name='object')

        div
            textarea(name="message" rows="12" cols="100")
                ='Bonjour Monsieur ' + destinationName + ',\n\n'
                ="Je vous écris cet email pour vous faire part d'une erreur dans la correction de mon examen de " + examName + '.\n\n'
                ="Merci de bien vouloir corriger. \n\n" 
                ="Cordialement,\n"
                = name
                = "\n\n\nLien vers l'examen :  " + url
        
        div(class="row mt-3")
            div(class="col-12")
                input(class="btn btn-warning float-end" type="submit", value="Envoyer l'email")
